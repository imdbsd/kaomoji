import styled from 'styled-components';

const Header = styled.nav`
  font-family: 'Pacifico', cursive;
  display: flex;
  font-size: 24px;
  justify-content: center;
  align-items: center;
  box-shadow: 6px 1px 10px 0px #888888;
  a {
    color: #fff;
  }
  a:hover {
    color: inherit;
  }
`;

export default Header;
